use Mix.Config

#
# Settings for use with Ecto can be overridden as follows:
#
# config :mail_address,
#   ecto_allow_address_literal: true,  # if yes, allow IP address literals.
#   ecto_allow_localhost: false,       # if yes, allow 'localhost' as domain.
#   ecto_allow_null: true,             # if yes, allow empty (null) address.
#   ecto_downcase_domain: true         # if yes, force domain name to lower case.
#   ecto_require_domain: true          # if yes, require a domain name to be present.
#
